# 숨은 박 — 구조·컨벤션

## 한 문장

- **규칙:** 들리는 박에 맞춰 탭한다. 박 소리는 점점 사라지고, 무음 속에서도 박을 맞추면 콤보와 배율이 오른다.
- **스킬:** 박자를 몸속에 유지하는 것(내적 템포). 시각 반응이 아니라 **예측**을 시험한다.

## 파일

```
index.html      게임 전부 (빌드 없음, 외부 라이브러리·에셋 없음)
tests/run.js    자동 플레이 테스트 (Playwright + window.__beat)
README.md       규칙·수치·이유
```

`index.html` 안의 순서:

| 구역 | 내용 |
|------|------|
| `DIFFS` | 난이도 표. **튜닝은 이 표만 고친다** |
| `Store` | 저장 어댑터 `window.storage` → `localStorage` → 메모리. **비동기다** |
| `Rec` / `loadAll` | 전체 기록(로컬) + 온라인 랭킹(Supabase REST) |
| `createEngine` | **순수 로직.** 오디오·그래픽·DOM 을 전혀 모른다 |
| 오디오 | `AudioContext`, lookahead 스케줄러, 합성 효과음 |
| 화면 | Canvas 2D 그리기 |
| 게임 상태 `G` | 모드 전환, 입력, 스케줄러 |
| 기록 화면 | 게임 오버 DOM 오버레이(`#over`) — 목록·이름 입력은 캔버스보다 DOM 이 맞다 |
| `window.__beat` | 자동 플레이·리플레이 훅 |

## 시계 규칙 (이 게임에서 가장 중요한 규칙)

- **시계는 `AudioContext.currentTime` 하나뿐이다.** 박 스케줄도 판정도 전부 이 시계 기준.
- **시계가 실제로 흐르기 시작한 뒤에 판을 시작한다.** `AudioContext` 는 `suspended` 동안
  `currentTime` 이 0에 멈춰 있고 `resume()` 은 비동기다. 멈춘 시계로 `t0` 를 잡으면
  첫 판만 카운트인이 얼어붙고 박이 통째로 틀어진다(실제로 겪은 버그).
  `state === "running"` 만 보는 것으로는 부족하다 — 크로미엄은 컨텍스트를 만들자마자
  `running` 이라고 답하면서 `currentTime` 은 한동안 0으로 둔다.
  **`currentTime` 이 두 번 다르게 읽혀야** 진짜 흐르는 것이다(`whenClockRunning`).
  그래서 `startRun()` 은 비동기이고, `window.__beat.reset()` 은 Promise 를 돌려준다.
- 입력 시각은 `performance.now()` 로 들어오므로 오디오 시계로 변환한다.
  변환 오프셋 `clockOffset` 은 판 시작 시 `syncClock()` 으로 **1회** 측정한다.
  이 측정도 시계가 깬 뒤에 해야 한다 — 죽은 시계로 재면 오프셋 전체가 틀어진다.
- 클릭음은 `AudioContext` 로 **미리 예약**한다. lookahead 100ms, 스케줄러 주기 25ms.
  `setTimeout` 으로 박을 만들지 않는다 — 타이머 지터가 곧 박의 지터가 되기 때문.
- `requestAnimationFrame` 은 **그리기에만** 쓴다. 게임 진행은 25ms 스케줄러가 한다.
  그래서 주사율이 판정에 영향을 주지 않는다.

## 엔진 계약

`createEngine(diff, t0)` 는 두 가지 입구만 갖는다.

- `advanceTo(t)` — 시각 `t` 까지 진행. 지나간 박은 놓침으로 확정된다.
- `tap(t)` — 시각 `t` 의 입력을 판정한다.

**`advanceTo` 의 호출 간격이 달라도 결과가 같아야 한다.** 이것이 주사율 독립성이자
리플레이 재현성의 근거다. 새 로직을 넣을 때 "지난 프레임 이후 흐른 시간"에 의존하는
판정을 만들면 이 성질이 깨진다. 확정은 반드시 절대 시각(`박 시각 + good`)으로 판단한다.

- 미판정 박은 **항상 정확히 1개**다. 박은 이전 박이 확정될 때 하나씩 생성된다.
  무음 길이가 콤보에 따라 달라지므로 박을 미리 다 만들어 둘 수 없기 때문이고,
  확정 시각이 결정적이므로 생성도 결정적이다.
- 엔진은 난수를 쓰지 않는다. 난수는 테스트 봇에만 있다.

## 그 밖의 컨벤션 (프로젝트 공통)

- 모든 움직임에 `dt` 를 곱한다(60fps 환산, `[0, 3]` 클램프).
- 오디오는 사용자 제스처(첫 탭) 이후 시작.
- UI 텍스트는 한국어.
- `prefers-reduced-motion` 존중 — 화면 흔들림 제거, 파편 수 감소.
- 시스템은 코어 루프 하나만. 리더보드는 재미가 확인된 뒤에 붙였다(원칙 9). **여기서 멈춘다.**
- **저장은 전부 비동기다.** `Store.get/set` 은 Promise 를 돌려준다 — 아티팩트 환경의
  `window.storage` 가 비동기이기 때문이다(stacker 에서 검증된 형태). 그래서 `G.best` 와
  `offsetMs` 는 0으로 시작하고 `loadAll()` 이 채운다. 모듈 최상단에서 동기로 읽으면 안 된다.
- **전체 랭킹은 배포판(GitHub Pages)에서만 동작한다.** Claude 아티팩트 미리보기는 CSP 가
  외부 `fetch`/XHR 을 전부 막는다. 미리보기에서 랭킹이 안 뜨는 것은 정상이고 Supabase
  문제가 아니다 — 그래서 실패 안내에 그 이유를 적어 둔다.
- **네트워크가 어떻게 죽든 게임과 로컬 기록은 그대로 돌아가야 한다.** 랭킹 요청은 6초에
  끊고 실패하면 안내로 떨어진다. 제출 실패도 게임을 막지 않는다 — 로컬 기록은 이미 저장돼 있다.
- 랭킹 캐시(`Rec.online`)는 30초간 신선하게 보고 다시 부르지 않는다. **실패한 것도**
  잠시 쉬었다 재시도해야 렌더 → 실패 → 렌더가 무한히 돈다.

## 테스트

```bash
npm install
npx playwright install chromium     # 브라우저가 이미 있으면 CHROMIUM_PATH 로 지정
npm test
```

수치는 **밸런스 확인용이지 재미의 증거가 아니다.** 재미는 직접 10판 해보고 판단한다.

테스트 `[7]` 은 "늦게 깨는 `AudioContext`" 를 주입해 첫 판 버그 조건을 만든다.
헤드리스 크로미엄은 5ms 만에 깨어나 그냥은 재현되지 않기 때문이다.
**회귀 테스트를 추가할 때는 고치기 전 코드에서 실제로 실패하는지 반드시 확인한다** —
`nextBeatTime` 에 맞춰 치는 검사는 내부 일관성만 증명하고 시계가 얼어붙은 것은 못 잡았다.
